{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "OPENAI_API_KEY",
      "description": "OpenAI API key for the AI product listing feature (get it from https://platform.openai.com/api-keys)",
      "requiredAtBoot": false
    }
  ]
}
